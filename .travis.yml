sudo: false

language: php
php:
  - 7.0
  - 7.1
  - 7.2
  - 7.3

cache:
  directories:
    - $HOME/.composer/cache

install:
  - composer install --prefer-dist

after_success:
  - travis_retry php vendor/bin/coveralls
